{
  "devDependencies": {
    "tailwindcss": "^3.4.4"
  },
  "scripts": {
    "build": "tailwindcss -i input.css -o index.css --minify",
    "start": "tailwindcss -i input.css -o index.css --watch"
  }
}
